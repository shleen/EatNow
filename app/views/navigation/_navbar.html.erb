  <nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-between">
  <%= link_to "EatNow", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>">Home<span class="sr-only">(current)</span></a>
      </li>
      <% if current_user.type == 'Staff' %>
        <% if current_user.role == 'owner' %>
          <li class='nav-item'>
            <%= link_to "Add Menu Item", new_menu_item_path, class: 'nav-link' %>
          </li>
        <% end %>
      <% else %>
      <li class="nav-item">
        <%= link_to "Checkout", order_index_path, class: "nav-link" %>
      </li>

      <% end %>
      <% if current_user.admin %>
        <li class='nav-item'><a class='nav-link text-danger'>Admin</a></li>
      <% end %>
    </ul>
    <ul class="navbar-nav">
      <li class="nav-item">
        <span class='nav-link'><%= current_user.email %></span>
      </li>
      <li class="nav-item">
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
      </li>
    </ul>
  </div>
</nav>
